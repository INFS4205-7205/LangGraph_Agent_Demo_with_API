# Images directory

Put the images you want to search here (supported: .jpg, .jpeg, .png, .webp, .gif).

After running `python -m api_demo.image_search_agent`, describe the image you want in natural language and the agent will try to find the best matches.
